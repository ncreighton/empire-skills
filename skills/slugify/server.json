{
  "name": "io.github.ncreighton/slugify",
  "description": "Use this skill whenever you need to convert an arbitrary string into a clean URL slug: lowercase, non-alphanumerics to single hyphens, collapse repeats, trim, optional max length on a word boundary. Deterministic, offline, no dependencies.",
  "version": "0.1.0",
  "packages": [
    {
      "registry_type": "pypi_or_git",
      "identifier": "slugify",
      "transport": {
        "type": "stdio"
      }
    }
  ],
  "_skill": {
    "entry": "skill",
    "input_schema": {
      "type": "object",
      "properties": {
        "text": {
          "type": "string",
          "description": "Text to slugify"
        },
        "max_length": {
          "type": "number",
          "description": "Max slug length (word-boundary trim)"
        }
      },
      "required": [
        "text"
      ]
    },
    "cost_tier": "free"
  }
}