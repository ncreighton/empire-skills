{
  "name": "io.github.ncreighton/meta-description",
  "description": "Use this skill to generate a clean SEO meta description from article body text: strips markdown/URLs, collapses whitespace, truncates to a max length (default 155) on a word boundary. Deterministic, offline.",
  "version": "0.1.0",
  "packages": [
    {
      "registry_type": "pypi_or_git",
      "identifier": "meta-description",
      "transport": {
        "type": "stdio"
      }
    }
  ],
  "_skill": {
    "entry": "skill",
    "input_schema": {
      "type": "object",
      "properties": {
        "text": {
          "type": "string",
          "description": "Input text"
        },
        "max_length": {
          "type": "number",
          "description": "Max chars (default 155)"
        }
      },
      "required": [
        "text"
      ]
    },
    "cost_tier": "free"
  }
}