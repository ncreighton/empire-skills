{
  "name": "io.github.ncreighton/smart-truncate",
  "description": "Use this skill to truncate text to a max length at a word boundary with an ellipsis, without cutting words in half. Ideal for meta descriptions (155 chars), social previews, and card summaries. Deterministic, offline.",
  "version": "0.1.0",
  "packages": [
    {
      "registry_type": "pypi_or_git",
      "identifier": "smart-truncate",
      "transport": {
        "type": "stdio"
      }
    }
  ],
  "_skill": {
    "entry": "skill",
    "input_schema": {
      "type": "object",
      "properties": {
        "text": {
          "type": "string",
          "description": "Text to truncate"
        },
        "max_length": {
          "type": "number",
          "description": "Max length incl ellipsis (default 155)"
        }
      },
      "required": [
        "text"
      ]
    },
    "cost_tier": "free"
  }
}